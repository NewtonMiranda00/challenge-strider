class FollowController {

    async follow(follower_id: String, session_user: String): Promise<UserType> {


    }

    async unfollow(follower_id: String, session_user: String) {

    }
}

export default new FollowController;